"""
The resources package og PyFynance is where all of the data that PyFynance relies on to operate is stored, along with
all database files that PyFyance generates or maintains

Currently the resources package stores the data for both the configuration and database services.
"""